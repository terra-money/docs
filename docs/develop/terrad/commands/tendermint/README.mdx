# terrad tendermint

[Tendermint](https://tendermint.com/) subcommands. 

```js Terrad
Tendermint subcommands

Usage:
  terrad tendermint [command]

Available Subcommands (click for more info):

  // link[3:138] /develop/terrad/commands/tendermint/terrad_tendermint_reset-state
  reset-state      Remove all the data and WAL
  // link[3:138] /develop/terrad/commands/tendermint/terrad_tendermint_show-address
  show-address     Shows this node's tendermint validator consensus address
  // link[3:138] /develop/terrad/commands/tendermint/terrad_tendermint_show-node-id
  show-node-id     Show this node's ID
  // link[3:138] /develop/terrad/commands/tendermint/terrad_tendermint_show-validator
  show-validator   Show this node's tendermint validator info
 // link[3:138] /develop/terrad/commands/tendermint/terrad_tendermint_unsafe-reset-all
  unsafe-reset-all (unsafe) Remove all the data and WAL, reset this node's validator to genesis state
// link[3:138] /develop/terrad/commands/tendermint/terrad_tendermint_version
  version          Print tendermint libraries' version

Flags:
  -h, --help   help for tendermint

Global Flags:
      --home string         directory for config and data (default "/Users/evan/.terra")
      --log_format string   The logging format (json|plain) (default "plain")
      --log_level string    The logging level (trace|debug|info|warn|error|fatal|panic) (default "info")
      --trace               print out full stack trace on errors

Use "terrad tendermint [command] --help" for more information about a command.
```

## Options

```sh Terrad
  -h, --help   help for terrad
```

## See also

* [Terrad commands](../terrad.mdx)