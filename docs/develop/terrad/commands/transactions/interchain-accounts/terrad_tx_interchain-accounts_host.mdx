# terrad tx interchain-accounts host

IBC interchain accounts host transaction subcommands.

## Usage

```sh Terrad
terrad tx interchain-accounts host [flags]
```

## Available Subcommands

Generate ICA packet data.

```sh Terrad
terrad tx interchain-accounts host generate-packet-data 
```

## Options inherited from parent commands

```sh Terrad
Global Flags:
      --chain-id string     The network chain ID
      --home string         directory for config and data (default "/Users/.terra")
      --log_format string   The logging format (json|plain) (default "plain")
      --log_level string    The logging level (trace|debug|info|warn|error|fatal|panic) (default "info")
      --trace               print out full stack trace on errors
```

## See also

* [terrad tx](../terrad_tx.mdx)	 - Transactions subcommands.